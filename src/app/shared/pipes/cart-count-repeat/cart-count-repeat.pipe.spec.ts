import { CartCountRepeatPipe } from './cart-count-repeat.pipe';

describe('CartCountRepeatPipe', () => {
  it('create an instance', () => {
    const pipe = new CartCountRepeatPipe();
    expect(pipe).toBeTruthy();
  });
});
